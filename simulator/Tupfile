include_rules

DEP_LIBS += $(TOP)/base/libbase.a

SRCS += A4988Simulator.cc
SRCS += Car.cc
SRCS += Ui.cc
SRCS += SimMain.cc

: foreach $(SRCS) |> !compile |>
: *.o $(DEP_LIBS) |> $(CC) $(LDFLAGS) %f -o %o |> sim
