include_rules

DEP_LIBS += ../base/libbase.a

SRCS += A4988Simulator.cc
SRCS += SimMain.cc

#LDFLAGS += -v -Wl,-v

: foreach $(SRCS) |> !compile |>
: *.o $(DEP_LIBS) |> $(CC) $(LDFLAGS) %f -o %o |> sim
