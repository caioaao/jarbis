TOP = $(TUP_CWD)

CFLAGS += -Wall
CFLAGS += -Wpedantic
CFLAGS += -Wextra
CFLAGS += -I$(TOP)
CFLAGS += -std=c++11

CFLAGS += -D _DO_ASSERT

CC = g++
AR = ar
ARFLAGS = crs

!compile = |> $(CC) $(CFLAGS) -c %f -o %o |> %B.o
