include_rules

DEP_LIBS += $(TOP)/base/libbase.a

SRCS += ConfigTest.cc
SRCS += LoggerTest.cc
SRCS += main.cc

: foreach $(SRCS) |> !compile |>
: *.o $(DEP_LIBS) |> $(CC) $(LDFLAGS) %f -o %o |> base_test